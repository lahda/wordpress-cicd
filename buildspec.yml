version: 0.2

phases:
  install:
    commands:
      - echo "Installation phase - no runtime needed for WordPress"
  pre_build:
    commands:
      - echo "Préparation du build..."
      - ls -la
  build:
    commands:
      - echo "Build en cours..."
      - echo "WordPress est prêt pour le déploiement"
  post_build:
    commands:
      - echo "Build terminé avec succès"

artifacts:
  files:
    - '**/*'